/**
 * @description: localstorage key
 */
export enum LOCAL_KEY {
  THEME = "theme", // website theme
  UPDATETIME = "update-time", // hotlist update time
  PRIMARYCOLOR = "primary-color", // theme color
  HOTHIDDEN = "hot-hidden", // Hide the hot list
}

/**
 * @description: theme mode
 */
export enum THEME {
  LIGHT = "light",
  DARK = "dark",
}
